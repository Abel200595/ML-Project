## Project Structure

ML-Project/
├── data/                  # dataset folder
│   ├── raw/               # raw data
│   └── processed/         # cleaned data
├── models/                # trained models
├── get_raw_data.py        # script for downloading / preparing data
├── train_model.py         # model training
├── app.py                 # front end
